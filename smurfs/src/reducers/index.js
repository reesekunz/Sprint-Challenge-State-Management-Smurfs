const initialState = {
  getsmurfsdata: [],
  test: "Testing to see if connected!"

};

export const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
